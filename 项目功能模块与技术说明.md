# 企业生命健康智能平台

## 一、项目概述

企业生命健康智能平台面向企业管理员和员工，提供健康档案、体检报告、风险识别、健康计划、心理健康、健康服务以及企业健康运营等能力。平台以员工健康数据为基础，以 Agent Runtime 和检索增强生成（RAG）为智能能力底座，同时提供管理员侧的用户、知识库、模型、Agent 和系统运行管理。

平台的核心目标是将“健康数据采集 → 指标解析 → 风险识别 → 个性化建议 → 计划执行 → 反馈再评估”串成可持续的健康管理闭环，而不是只提供一次性的聊天问答。

## 二、功能模块说明

### 1. 登录、认证与权限

- 账号密码登录、记住账号、密码显示/隐藏。
- JWT 会话认证，前端请求通过统一 API Client 自动携带令牌。
- 基于角色的访问控制（RBAC），区分员工端和管理员端菜单、页面及接口权限。
- 登录失效、退出登录、个人会话清理和受保护路由跳转。

### 2. 员工工作台

- 员工首页展示总体健康状态、健康风险、当前健康计划和心理状态。
- 今日建议、待办任务、近期健康动态和快捷入口。
- 通过 Dashboard 聚合接口读取真实的用户健康数据；没有数据时显示明确的空状态。

### 3. AI 健康助手

- 支持多轮对话、会话列表和历史消息。
- 通过 Agent Runtime 进行意图识别、Agent 路由、工具调用和模型生成。
- 可结合健康档案、体检指标、风险结果、健康计划等个人健康上下文。
- 支持流式输出和 Agent 执行轨迹展示；工具权限由数据授权和 Agent 绑定共同约束。

### 4. 健康档案与个人中心

- 查看和编辑姓名、邮箱、手机号、部门、职位、个人简介等资料。
- 支持头像上传，头像文件通过 MinIO 保存并返回可访问地址。
- 员工个人中心包含账号安全和修改密码能力；管理员个人中心提供基本资料、修改密码和通知设置。
- 用户名、角色、所属企业等身份字段由后端返回并按权限只读。

### 5. 体检报告管理

- 上传 PDF 体检报告并记录原始文件、解析状态和报告元数据。
- 支持原生文本/表格 PDF 和扫描 PDF 的混合解析。
- 当前解析链路为：Docling/原生文本 → 高 DPI 页面渲染 → 图像预处理 → RapidOCR → 质量评估 → PaddleOCR fallback → OCR 文本与 bbox/layout → 通用体检指标抽取。
- 指标不依赖固定白名单，动态保存指标名称、原始值、单位、参考范围、状态、方法和 OCR 来源等字段。
- 支持 `1.45`、`<4.0`、`≥90`、阴性、弱阳性、`+ / ++ / +++`、未检出等非浮点结果。
- 指标详情可追溯到报告、页码和 OCR 来源，并在低置信度时提示人工核对。
- 体检报告分析 Agent 只读取已解析的结构化指标，不重复解析 PDF。

### 6. 风险管理

- 员工侧查看健康风险、风险等级、风险来源和建议。
- 管理员侧提供风险预警、风险案例、处理状态及风险运营视图。
- 风险计算可综合体检指标、健康档案和已授权的可穿戴/行为数据。
- 风险结果可进入后续健康计划和干预流程。

### 7. 健康计划

- 创建、查看和执行个性化健康计划。
- 支持睡眠改善、运动、饮食、心理等计划类型。
- 以任务、目标、完成状态和反馈记录计划执行情况。
- 可基于复评结果调整计划，形成“计划—执行—反馈—再评估”闭环。

### 8. 心理健康

- 心理状态记录、心理测评结果和状态趋势。
- 结合心理健康 Agent 生成非诊断性的支持建议。
- 对高风险结果保留提示和人工/服务转介入口。

### 9. 健康服务

- 展示企业提供的健康服务、预约/报名入口和服务状态。
- 管理员可维护健康活动、报名状态、参与人数和运营数据。

### 10. 数据授权与隐私

- 员工可管理健康档案、体检报告、睡眠、运动、心率等数据授权。
- Agent 和工具按授权范围读取数据，越权访问由后端拒绝。
- 授权记录、通知设置和敏感操作均由后端持久化。

### 11. 管理员企业驾驶舱

- 企业员工总数、健康档案覆盖率、需关注员工、高风险员工等 KPI。
- 健康状态分布、风险趋势、待处理事项和系统运行状态。
- 数据来自企业用户、档案、风险、体检、活动和 Agent 运行记录等后端聚合查询。

### 12. 用户与员工管理

- 管理员工账号、姓名、部门、职位、角色和启用状态。
- 支持管理员创建、编辑、启用/停用、删除和重置密码。
- 员工本人修改密码使用独立接口，不复用管理员重置密码权限。

### 13. 员工健康分析

- 按企业整体或部门查看健康数据覆盖、风险关注和体检异常聚合结果。
- 部门选项与当前筛选后的统计结果分离，列表来自真实后端数据并去重。
- 支持部门筛选、趋势和健康状态分布，管理员只能看到授权范围内的匿名聚合数据。

### 14. 体检管理与健康活动运营

- 管理员查看报告处理状态、解析结果和人工复核信息。
- 创建、发布、报名、截止、取消和结束健康活动。
- 展示活动时间轴、报名动态、活动卡片和状态汇总。

### 15. 知识库与 RAG 管理

- 知识库创建、文档上传、解析、切分、Embedding 和索引状态管理。
- 文档和 Chunk 保留来源信息，便于检索结果追踪。
- 通过 MinIO 保存原始文档，通过 Qdrant 保存向量及元数据。
- 支持绑定 Agent、检索测试和 Top-K 结果查看。

### 16. Agent 管理

- 查看 Agent 总数、启用数量、今日运行次数和最近异常次数。
- Agent 卡片选择、正式/测试分类、分页和详情工作台。
- 详情包含基本信息、能力配置、绑定模型/知识库/工具、运行监控和测试调试。
- Agent 状态、绑定关系及运行数据来自数据库和 `agent_runs`，不依赖前端演示统计。
- 测试调试通过当前 Agent Runtime 执行；不支持的流程明确显示“未执行”。

### 17. 模型管理

- 管理模型 Provider、模型配置、默认模型和可用状态。
- Agent 通过绑定关系使用模型，模型密钥由服务端配置管理，不下发到前端。

### 18. 系统监控与管理员个人中心

- 查看 PostgreSQL、Redis、Qdrant、MinIO、FastAPI、Agent Runtime 等服务状态。
- 查看请求/运行统计、异常状态和基础运行指标。
- 管理员个人中心支持本人资料、头像、密码和四类必要通知开关。

## 三、技术架构说明

### 1. 总体架构

```text
Electron / Browser
        ↓
React + TypeScript + Vite
        ↓  /api 代理
FastAPI + Uvicorn
        ├── JWT / RBAC / API Routes
        ├── Agent Runtime / Tool Calling
        ├── Health Report Parser / OCR
        └── RAG Retrieval Service
        ↓
PostgreSQL · Redis · MinIO · Qdrant
```

### 2. 前端技术

- React + TypeScript，使用 Vite 构建和开发服务器。
- Ant Design 组件与项目自定义 CSS，统一采用白色卡片、浅灰边框、绿色主色和圆角布局。
- Axios 统一请求封装、JWT interceptor、错误处理和 API 类型定义。
- Zustand 等状态管理用于登录用户、页面状态和会话数据。
- ECharts/图表组件用于趋势、分布和监控可视化。
- 支持 Electron 桌面端运行，也支持浏览器和局域网访问。

当前前端开发端口为 **5341**，Vite 监听 `0.0.0.0`；前端 `/api` 请求代理到后端 `8002`。

### 3. 后端技术

- Python FastAPI + Uvicorn，接口前缀为 `/api/v1`。
- SQLAlchemy 异步 ORM + asyncpg 访问 PostgreSQL。
- JWT 认证与 `current_user` 依赖获取当前用户，RBAC 依赖控制管理员接口。
- 密码使用项目统一的密码 hash/verify 工具处理，数据库只保存 hash，不保存明文密码。
- 后台任务用于报告解析、知识文档处理和长耗时 Agent 运行。

### 4. 数据存储

- **PostgreSQL**：用户、角色、档案、体检报告、指标、风险、计划、活动、通知、Agent、绑定关系、运行记录和知识库元数据。
- **Redis**：缓存、任务状态、短期会话/队列等运行态数据。
- **MinIO**：体检报告、知识库原始文档、用户头像等对象文件。
- **Qdrant**：知识库 Chunk 的向量索引和检索元数据。

### 5. 体检报告解析链路

```text
PDF 上传
  → 文件保存与报告记录
  → 原生文本/表格检测
  → 350 DPI 页面渲染
  → 图像预处理
  → RapidOCR
  → 质量评估
  → PaddleOCR fallback（扫描件/低质量结果）
  → bbox + confidence + page_number
  → Y 坐标聚合行、X 坐标排序
  → 通用指标候选抽取
  → health_report_items
  → 风险/报告分析 Agent
```

解析器不绑定某一家医院或固定指标名称，最低接受条件是 `item_name + raw_value`。OCR 低置信度数据会保留来源和提示，不把 OCR 失败直接等价为整份 PDF 失败。

### 6. RAG 知识基础设施

```text
MinIO 原始文档
  → Document 元数据
  → 文本解析与 Semantic Chunk
  → Embedding
  → Qdrant 向量索引
  → KnowledgeSearchTool Top-K Retrieval
  → Agent 领域授权过滤
  → Chunk 来源/页码证据
  → Agent 生成“个人健康上下文 + 专业知识证据”回答
```

知识库与 Agent、Tool、Model 使用独立绑定关系。专业 Agent 只能检索被授权的知识域；员工个人数据还要额外经过数据授权检查。

### 7. Agent Runtime 与业务闭环

- Supervisor 负责意图识别和专业 Agent 路由。
- Report、Risk、Sleep、Mental Health、Health Plan 等 Specialist Agent 通过 Tool Calling 获取结构化业务数据。
- 运行记录写入 `agent_runs`，保存状态、耗时、工具调用和错误信息，用于管理员监控。
- Risk Agent、Mental Health Agent 和 Health Plan Agent 可组成“采集 → 识别 → 分析 → 建议 → 计划 → 执行反馈 → 再评估”流程。
- 模型通过 Model Provider/Model Config 绑定，不在前端写死模型密钥或运行统计。

### 8. 安全与隐私

- 所有业务接口通过 JWT + RBAC 保护。
- 当前用户身份从 JWT 解析，前端不能通过传入任意 `user_id` 修改他人资料。
- 密码输入不写入普通日志、通知和审计正文；修改密码成功后清理会话并要求重新登录。
- CORS 仅允许本机及当前局域网前端来源；生产环境应进一步收敛域名和来源。
- 健康数据按授权范围访问，管理员侧统计优先使用匿名聚合结果。

### 9. 部署与运行

- Docker Compose 编排 PostgreSQL、Redis、Qdrant、MinIO 和 Backend。
- Backend 容器内部监听 `8000`，主机映射为 `8002`。
- 前端由 Vite/Electron 启动，主机监听 `5341`。
- 根目录 `start-platform.bat` 负责启动基础设施、后端和桌面前端，并在失败时保留错误窗口。

访问地址：

- 本机前端：`http://127.0.0.1:5341/`
- 局域网前端：`http://192.168.0.118:5341/`
- 后端健康检查：`http://127.0.0.1:8002/health`
- API 文档：`http://127.0.0.1:8002/docs`

## 四、主要接口分组

- 认证：`/api/v1/auth/*`
- 员工资料与档案：`/api/v1/users/*`、`/api/v1/health/*`
- Dashboard：`/api/v1/dashboard/*`
- 体检报告：`/api/v1/health-reports/*`
- 风险、计划、服务、心理：`/api/v1/risks/*`、`/api/v1/health-plans/*`、`/api/v1/health-services/*`、`/api/v1/mental-health/*`
- Agent Runtime：`/api/v1/agents/*`、`/api/v1/agent-runs/*`
- 知识库与检索：`/api/v1/knowledge-bases/*`、`/api/v1/documents/*`、`/api/v1/retrieval/*`
- 管理员：`/api/v1/admin/*`
- 通知与授权：`/api/v1/notifications/*`、`/api/v1/data-authorizations/*`

## 五、当前建设边界

当前核心业务、认证、健康档案、报告解析、RAG 基础设施、Agent Runtime 和管理员管理页面已经形成可运行骨架。部分高级运营图表、复杂策略编排和部分 Agent 的生产级模型效果仍取决于实际数据库数据、模型配置、知识文档数量和外部服务可用性；当真实数据不足时，界面应展示空状态或“未配置”，不应把演示数据当成生产统计。

