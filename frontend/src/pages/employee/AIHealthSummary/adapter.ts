import type { HealthSummary } from '../../../api/healthSummary'
export interface SummaryMetric { label:string; value:string; status:'正常'|'需要关注'|'风险'; source:string }
export interface SummaryViewModel { overall:string; metrics:SummaryMetric[]; attention:string[]; suggestions:string[]; updatedAt:string }
const asList=(v:unknown):string[]=>Array.isArray(v)?v.map(String):v==null||v===''?[]:[String(v)]
export function adaptHealthSummary(summary:HealthSummary|null):SummaryViewModel|null{if(!summary)return null;const raw=summary as unknown as Record<string,unknown>;const metrics:SummaryMetric[]=[];const f=raw.findings;if(f&&typeof f==='object'&&!Array.isArray(f)){const x=f as Record<string,unknown>;if(x.bmi!=null)metrics.push({label:'BMI',value:String(x.bmi).replace(/^BMI\s*/i,''),status:'正常',source:'Health Profile'});if(x.sleep!=null)metrics.push({label:'睡眠',value:String(x.sleep).replace(/^平均睡眠\s*/i,''),status:'需要关注',source:'Wearable'});if(x.exercise!=null)metrics.push({label:'运动',value:String(x.exercise).replace(/^平均每日运动\s*/i,''),status:'正常',source:'Wearable'});if(x.heart_rate!=null)metrics.push({label:'心率',value:String(x.heart_rate).replace(/^静息心率\s*/i,''),status:'正常',source:'Wearable'})}return{overall:String(raw.overall||'近期健康指标保持稳定，建议持续关注睡眠情况。'),metrics,attention:asList(raw.attention),suggestions:asList(raw.suggestions),updatedAt:String(raw.updated_at||new Date().toLocaleDateString('zh-CN'))}}
