import {Tag} from 'antd';import type {SummaryMetric} from './adapter';export function HealthMetricCard({metric}:{metric:SummaryMetric}){const color=metric.status==='风险'?'red':metric.status==='需要关注'?'gold':'green';return <div className="summary-metric"><span>{metric.label}</span><strong>{metric.value}</strong><Tag color={color}>{metric.status}</Tag><small>来源：{metric.source}</small></div>}
